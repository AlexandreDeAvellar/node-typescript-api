import { Controller, Get } from '@overnightjs/core';

// Explicar o Controller que vai ser colocado aqui.
export class ForeCastController {
    public getForecastForLoggedUser(): void{
        // Explicar o GET que vai ser colocado aqui
    }
}